// Первое задание
#include "stdafx.h"
#include <iostream>
#include <math.h>
#include <cmath>

void main1()
{
	setlocale(LC_CTYPE, "Russian");
	setlocale(LC_ALL, "Russian");

	double x, y, p = 3.14;
	x = 0; // Fix error code C4700
	printf("Введите число: ");
	y = (sin(p * pow(x, 2)) + log(pow(x, 2))) / (sin(p * pow(x, 2))) + (sin(x)) + log(pow(x, 2)) + pow(x, 2) + exp(cos(x));
	printf("\n Ответ: result=%.2lf\n, y");
} // но чет оно все равно не работает... 
  // Программа "[18784] ConsoleApplication2.exe" завершилась с кодом 0 (0x0). 

// Второе задание
// #include "stdafx.h"
// #include <iostream>
// #include <math.h>
// #include <cmath>

void main2()
{
	setlocale(LC_CTYPE, "Russian");
	setlocale(LC_ALL, "Russian");
	
	double a, n, P = 3.14;


}























